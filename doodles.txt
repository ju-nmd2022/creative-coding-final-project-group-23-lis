Happy - Eyebrows up, Mouth corners up
Sad - Closed eyes, Mouth corners down
Angry - Mouth open, eyebrows down
Surprised - Open mouth, eyebrows up
Boredom/Sleepy - Closed eyes


function generateDynamicArt(emotion) {
  const canvas = document.querySelector("canvas");

  const ctx = canvas.getContext("2d");

  const videoElement = document.getElementById("video");
  const displaySize = {
    width: videoElement.videoWidth,
    height: videoElement.videoHeight,
  };
  faceapi.matchDimensions(canvas, displaySize);

  // Drawing logic based on emotion
  if (emotion === "happy") {
    ctx.fillStyle = "yellow";
    ctx.beginPath();
    ctx.arc(150, 150, 50, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "black";
    ctx.font = "30px Arial";
    ctx.fillText("Smiling Sun", 100, 250);
  } else if (emotion === "sad") {
    ctx.fillStyle = "blue";
    ctx.beginPath();
    ctx.arc(150, 150, 50, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "white";
    ctx.font = "30px Arial";
    ctx.fillText("Raindrop", 110, 250);
  } else if (emotion === "angry") {
    ctx.fillStyle = "red";
    ctx.beginPath();
    ctx.arc(150, 150, 50, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "black";
    ctx.font = "30px Arial";
    ctx.fillText("Angry Flame", 90, 250);
  }
  return emotion[randomIndex];
}



let simplex = new SimplexNoise(); // Use Simplex or Perlin Noise
let noiseScale = 0.01; // Scale of the noise

function generateNoisePattern(x, y) {
  return simplex.noise2D(x * noiseScale, y * noiseScale);
}
